name: size-label
on: pull_request
permissions: write-all
jobs:
  size-label:
    runs-on: ubuntu-latest
    steps:
    - name: size-label
      uses: "jmnote/size-label-action@bugfix-403-forbidden"
      env:
        GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
        DEBUG_ACTION: true
